<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <!-- T3.2: camera permission for QR scanning (mobile_scanner). The
         scanner UI gracefully degrades when the user declines, but
         the manifest must declare it so the runtime prompt fires. -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <!-- T3.2: hardware feature marked optional so devices without a
         camera (TVs, etc.) can still install the APK. -->
    <uses-feature android:name="android.hardware.camera" android:required="false"/>
    <!-- T3.2: usesCleartextTraffic allows plain http:// so LAN servers
         (the most common setup for friends playing locally) work without
         an explicit network-security-config. Production HTTPS via the
         T2.5 Caddy stack is unaffected. -->
    <application
        android:label="Scythe Companion"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize"
            android:enableOnBackInvokedCallback="true">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- T3.2 NOTE: an external scythe://join deep-link
                 intent-filter was considered and deliberately NOT added:
                 nothing handles an incoming VIEW intent yet (no app_links
                 plugin, no go_router route for it), so an external QR app
                 launching us would land on the router's error page. The
                 in-app scanner (JoinRoom → Scan QR) doesn't need it.
                 Revisit in T3.5 if external-scan launch is wanted. -->
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
</manifest>
